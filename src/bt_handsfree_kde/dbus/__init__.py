"""Everything that speaks a D-Bus protocol: the clients the app consumes and the servers it exports.

No module in this package creates widgets; the UI modules at the package root wire
these clients and servers to windows and the tray.
"""
